
public class points {
    public static void main(String[] args) {
        // java always calls by value

        // Strings are immutable

        // we can swap numbers using XOR operator

        // in shallow copy (changes in the prev do reflect in new)
        // (we pass the variable/object by refrence)

        // in deep copy new object/variable is created and values are copied
        // (changes in the prev do not reflect in new)

        // Lazy copy means:
        // You don’t immediately copy the original object.
        // Instead, you share the same object reference.
        // You make the copy only when someone tries to modify it.

        //we dont need to write destructor in java 
        //because of garbage collector

        //parent refrence = child obj (allowed in java)
    }

}
